[project]
name = "learning-sqla"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "psycopg>=3.2.2",
    "sqlalchemy>=2.0.34",
]

[tool.uv]
dev-dependencies = [
    "alembic>=1.13.3",
]
